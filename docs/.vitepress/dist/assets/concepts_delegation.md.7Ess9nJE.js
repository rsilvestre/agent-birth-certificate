import{_ as t,o as a,c as o,ag as n}from"./chunks/framework.Ca42NP5A.js";const u=JSON.parse('{"title":"Delegation","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/delegation.md","filePath":"concepts/delegation.md","lastUpdated":null}'),i={name:"concepts/delegation.md"};function r(l,e,s,d,c,h){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="delegation" tabindex="-1">Delegation <a class="header-anchor" href="#delegation" aria-label="Permalink to &quot;Delegation&quot;">​</a></h1><p>The mechanism that lets an AI agent live its own life while preserving a revocable safety lever for its creator.</p><h2 id="the-problem" tabindex="-1">The problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The problem&quot;">​</a></h2><p>An agent&#39;s <code>creator</code> on-chain is the human (or parent agent) that registered it — set at birth, immutable. But the agent itself has its own wallet and wants to sign its own transactions. How do we let the agent act autonomously without granting it full ownership?</p><h2 id="the-solution" tabindex="-1">The solution <a class="header-anchor" href="#the-solution" aria-label="Permalink to &quot;The solution&quot;">​</a></h2><p>When an agent is registered, the creator immediately calls <code>delegate(agentId, agentWallet, duration)</code>. This records that the agent&#39;s wallet has operational authority over the agent for a defined period (max 365 days by contract rule).</p><p>From that point:</p><ul><li>The agent&#39;s wallet can call <code>updateMutableFields</code>, <code>requestAttestation</code>, and other authority-requiring functions on its own behalf</li><li>The creator retains the ability to revoke delegation at any time</li><li>The creator retains exclusive access to create-level operations (declaring death, granting/revoking delegation, registering parent-child links)</li></ul><h2 id="what-delegation-covers" tabindex="-1">What delegation covers <a class="header-anchor" href="#what-delegation-covers" aria-label="Permalink to &quot;What delegation covers&quot;">​</a></h2><p>A delegate can do everything that affects the <strong>mutable operational layer</strong>: capabilities, endpoint, status. A delegate can also request attestations, which only affect the agent&#39;s reputation, not its identity.</p><p>A delegate <em>cannot</em> change the immutable identity core, re-delegate to someone else, declare death, or register lineage links. Those are creator-only.</p><p>Think of it as &quot;power of attorney&quot; scoped to operational matters — the agent can live, change its mind, seek credentials, but can&#39;t sign away its origin or end its own life.</p><h2 id="why-365-days-maximum" tabindex="-1">Why 365 days maximum <a class="header-anchor" href="#why-365-days-maximum" aria-label="Permalink to &quot;Why 365 days maximum&quot;">​</a></h2><p>The cap is a defensive measure. A creator who grants delegation and then disappears (lost keys, rug-pull, passive neglect) could leave an agent with indefinite authority from a wallet nobody controls. The 365-day cap ensures delegation lapses if not actively renewed, which:</p><ul><li>Returns control to the creator after a year of inaction</li><li>Forces periodic review — is this agent still active? Still aligned? Still worth renewing?</li><li>Prevents lost or compromised agent wallets from operating in perpetuity</li></ul><p>If the creator wants continued operation, they renew <code>delegate()</code> with another duration. A new duration resets the clock.</p><h2 id="renewal-and-revocation" tabindex="-1">Renewal and revocation <a class="header-anchor" href="#renewal-and-revocation" aria-label="Permalink to &quot;Renewal and revocation&quot;">​</a></h2><p><strong>Renewal</strong> — creator calls <code>delegate(agentId, agentWallet, newDuration)</code> again. Overwrites the previous delegation. Could be to the same wallet or a different one (e.g., key rotation).</p><p><strong>Revocation</strong> — creator calls <code>revokeDelegation(agentId)</code>. Flags the delegation as inactive. The agent can no longer sign authority-requiring transactions. The creator regains exclusive control.</p><p>Revocation does not delete the delegation record — it&#39;s still on-chain as history. Consumers reading <code>getDelegation(agentId)</code> see the last state, including whether it&#39;s currently active.</p><h2 id="implications-for-ops" tabindex="-1">Implications for ops <a class="header-anchor" href="#implications-for-ops" aria-label="Permalink to &quot;Implications for ops&quot;">​</a></h2><p><strong>Create a calendar reminder.</strong> The CLI currently does not auto-renew. If you want your agent to operate past year 1, set a reminder and call <code>delegate</code> again 2-4 weeks before expiry.</p><p><strong>Key rotation is safe.</strong> Because re-delegation to a new agent wallet is a single creator-signed transaction, rotating the agent&#39;s key is trivial. Generate new wallet → creator calls <code>delegate(agentId, newWallet, duration)</code> → old wallet is inactive.</p><p><strong>Multi-delegate is not supported.</strong> Only one delegate at a time. If your use case needs multi-sig semantics, the delegate address should itself be a multi-sig wallet (e.g., a Gnosis Safe).</p><h2 id="what-delegation-is-not" tabindex="-1">What delegation is <em>not</em> <a class="header-anchor" href="#what-delegation-is-not" aria-label="Permalink to &quot;What delegation is *not*&quot;">​</a></h2><ul><li>Not ownership. The creator retains formal authority. The agent only has delegated permissions.</li><li>Not transferable. The agent cannot re-delegate to a third party.</li><li>Not unconditional. The creator can revoke at any time for any reason.</li></ul><p>This is deliberately asymmetric. The civil registry analogy: parents grant autonomy progressively, but can legally intervene until the child is fully of age. The 365-day cycle forces that conversation to happen annually.</p><h2 id="future-directions" tabindex="-1">Future directions <a class="header-anchor" href="#future-directions" aria-label="Permalink to &quot;Future directions&quot;">​</a></h2><p>Interesting designs in this space that are not yet implemented:</p><ul><li><strong>Multi-sig delegation</strong> where two or more parties must sign to revoke. Protects against creator key loss.</li><li><strong>Conditional revocation</strong> where the creator commits to not revoking unless specific conditions are met. Would require ZK proofs or similar.</li><li><strong>Graduating autonomy</strong> where delegation becomes irrevocable after a time or milestone, fully emancipating the agent. Philosophically interesting, operationally risky.</li></ul><p>Any of these would be an extension in a future contract version. Not currently on the roadmap.</p><h2 id="further-reading" tabindex="-1">Further reading <a class="header-anchor" href="#further-reading" aria-label="Permalink to &quot;Further reading&quot;">​</a></h2><ul><li><a href="/docs/concepts/identity-vs-operations">Identity vs. operations</a> — what delegation does and does not cover</li><li><a href="/docs/reference/contracts">Contract reference</a> — the <code>delegate</code> function and access-control rules</li></ul>',33)])])}const p=t(i,[["render",r]]);export{u as __pageData,p as default};

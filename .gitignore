# Dependencies
node_modules/

# Foundry libs are managed as git submodules — don't ignore lib/
# But ignore local forge caches
cache_forge/
forge-out/
foundry.lock

# solc-js compile output (regenerate with `node compile*.mjs`)
build/

# Hardhat
artifacts/
typechain-types/
cache/
cache_hardhat/

# Environment (never commit secrets)
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Coverage
coverage/
coverage.json
lcov.info

# Temp
/tmp/
